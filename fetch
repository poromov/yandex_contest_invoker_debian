#!/bin/bash -e

cd "$(dirname "$0")"

. ./config.sh

for i in "${libs[@]}"
do
    ./clean "$i"
    if [[ -d $i ]]
    then
        pushd "$i"
        git fsck
        git pull
        popd
    else
        git clone "$repo/$lib_prefix$i.git" "$i"
    fi
done
